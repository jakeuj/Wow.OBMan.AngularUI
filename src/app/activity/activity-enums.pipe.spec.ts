import { ActivityEnumsPipe } from './activity-enums.pipe';

describe('ActivityEnumsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityEnumsPipe();
    expect(pipe).toBeTruthy();
  });
});
