import { GetActivityTitlePipe } from './get-activity-title.pipe';

describe('GetActivityTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetActivityTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
